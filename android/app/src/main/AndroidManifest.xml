<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Ads and the UMP consent form are the only things here that touch the
         network; every SIM/network/device value is read on device. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Connectivity tab: active transport, metered state, DNS, bandwidth. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Runtime permission behind everything TelephonyManager and
         SubscriptionManager report: carriers, MCC/MNC, network type, signal. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- Runtime permission behind the SIM's own line number on API 30+.
         Requested together with READ_PHONE_STATE; the app degrades to
         "unavailable" when it is denied. -->
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS"/>

    <!-- Cell tower identities (Cell ID, TAC, PCI, bands) — Android treats a
         cell id as a location, so getAllCellInfo is gated on fine location.
         Requested at runtime only when the user opts in to the cell towers
         section, never at startup. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Data usage totals via NetworkStatsManager. Special access granted
         from the Usage Access settings screen, again only when the user opts
         in to the data usage section. -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>

    <!-- The app is useful only on devices with telephony, but required="false"
         keeps Wi-Fi tablets able to install it and see device/connectivity
         info rather than being filtered off the listing. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:localeConfig="@xml/locales_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Home screen widget: carrier + network + signal per SIM. -->
        <receiver
            android:name=".SimInfoWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_sim_info"/>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9785628886941445~2184752117"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
