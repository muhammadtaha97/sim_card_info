<?xml version="1.0" encoding="utf-8"?>
<!-- Declares the app's languages to Android 13+, which is what makes the
     per-app language option appear in system Settings. Must stay in sync
     with AppLocalizations.supportedLocales — test/config_test.dart asserts
     the two sets agree. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="ar"/>
    <locale android:name="bn"/>
    <locale android:name="de"/>
    <locale android:name="en"/>
    <locale android:name="es"/>
    <locale android:name="fr"/>
    <locale android:name="hi"/>
    <locale android:name="it"/>
    <locale android:name="ja"/>
    <locale android:name="ko"/>
    <locale android:name="pt"/>
    <locale android:name="ru"/>
    <locale android:name="tr"/>
    <locale android:name="ur"/>
    <locale android:name="zh"/>
</locale-config>
